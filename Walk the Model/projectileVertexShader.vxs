#version 330 core
layout (location = 0) in vec3 aPos;

uniform mat4 projection;
uniform mat4 view;
uniform float translate;

void main()
{
   gl_Position = projection * view * vec4(aPos.x + translate, aPos.y + translate, aPos.z + translate, 1.0);
}